<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        android:id="@+id/favorite_card_item"
        android:layout_width="165dp"
        android:maxWidth="165dp"
        android:layout_height="165dp"
        android:layout_marginHorizontal="4dp"
        app:cardElevation="4dp"
        app:cardCornerRadius="12dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        >
        <ImageView
            android:id="@+id/favorite_card_item_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter"
            app:layout_constraintDimensionRatio="1:1"
            android:adjustViewBounds="false"
            android:importantForAccessibility="no" />


    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/favorite_list_card_item_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:text="@string/favorite_card_item_name"
        android:textSize="14sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/favorite_card_item" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/favorite_list_card_item_add_to_shopping_cart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/favorite_list_card_item_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:icon="@drawable/baseline_add_shopping_cart_24"
        app:cornerRadius="4dp"
        android:text="@string/favorite_list_add_to_shopping_cart" />

</androidx.constraintlayout.widget.ConstraintLayout>